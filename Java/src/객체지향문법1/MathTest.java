package 객체지향문법1;

public class MathTest {
    public static void main(String[] args) {

        // Math클래스의 모든 메소드는 static하기 때문에, Math.메소드() 로 사용
        int value = Math.abs(-5);
        System.out.println(value);

    }
}
